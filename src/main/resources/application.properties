spring.output.ansi.enabled = always

# HTTP Server Port
server.port = 8181

# Connect to Keycloak (Auth) Server
spring.security.oauth2.resourceserver.jwt.issuer-uri = http://localhost:8080/realms/myrealm
spring.security.oauth2.resourceserver.jwk-set-uri = ${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs

keycloak.realm = myrealm
keycloak.domain = localhost:8080
keycloak.adminClientId = admin-cli
keycloak.adminClientSecret = vyBv4a52YYN97cvRkZYGtZ98pFmtjZlV
keycloak.urls.auth = http://${keycloak.domain}

keycloak.auth-server-url = http://${keycloak.domain}
#keycloak.resource = democlient